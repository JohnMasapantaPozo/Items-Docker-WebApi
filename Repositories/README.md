### MongoDB Client

```text
This is the adaptor or so-called driver that will interface with the MongoDB database. Created by MongoDB itself.

For instantiating our database we can either install a database or run
a database instance in a docker container.
```

```shell
dotnet add package MongoDB.Driver -v 2.11.4